# Environment variables for local development

# OpenAQ API Key (get from https://explore.openaq.org)
OPENAQ_API_KEY=your_openaq_api_key_here

# Frontend origin for CORS
FRONTEND_ORIGIN=http://localhost:3000

# Database connection (for Docker Compose PostgreSQL)
CONNECTION_STRING=Host=localhost;Database=sarajevoair;Username=dev;Password=dev

# ASP.NET Core environment
ASPNETCORE_ENVIRONMENT=Development

# Optional: Redis cache connection string (if implementing Redis caching)
# REDIS_CONNECTION_STRING=localhost:6379